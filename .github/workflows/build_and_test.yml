name: Build and Test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Prevent duplicate runs on the same ref and cancel in-progress on PR updates to save CI minutes
concurrency:
  group: build-and-test-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build_and_test:
    # Was ubuntu-latest (GitHub-hosted) -- switched to the self-hosted `main`
    # runner pool. Dockerfile/pre-commit.Dockerfile now pull their base image from
    # the root-mirror ECR (services account, 856965016623), which needs the ECR
    # login below; that role (services-k8s-service-account-github-actions-runner)
    # is only reachable via the self-hosted runners' Kubernetes-service-account-
    # federated identity, not GitHub's native Actions OIDC -- ubuntu-latest 401s on
    # the pull even with `permissions: id-token: write` set. Matches every other
    # repo already migrated to root-mirror (e.g. patient-matching-service,
    # mcp-server-gateway).
    runs-on: main

    steps:
      # Checks-out your repository
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      # Both the pre-commit step below (builds pre-commit.Dockerfile) and the
      # `up`/`tests` steps (build the main Dockerfile via docker-compose) FROM
      # root-mirror/python, which lives in the services account (856965016623) --
      # a separate account from wherever this repo's own images publish to.
      - name: ECR login (services account, for root-mirror base image pulls)
        uses: ./.github/actions/ecr-login

      # The self-hosted `main` runner image doesn't ship `make` or a modern
      # Docker Compose CLI plugin -- unlike ubuntu-latest, which has both
      # preinstalled. Matches the pattern already used by other repos' `main`
      # runner CI (e.g. patient-matching-service, helix.providersearch).
      - name: Install make
        run: |
          sudo apt-get update && sudo apt-get install -y make
          sudo apt-get clean

      # Verified against the release's published sha256 checksum, not just
      # downloaded and run blind.
      - name: Install Docker Compose CLI plugin
        run: |
          set -eu
          DC_VERSION=v2.24.6
          DC_ASSET="docker-compose-linux-x86_64"
          DC_URL="https://github.com/docker/compose/releases/download/${DC_VERSION}/${DC_ASSET}"
          mkdir -p ~/.docker/cli-plugins/
          curl -SL "${DC_URL}" -o /tmp/docker-compose
          curl -SL "${DC_URL}.sha256" -o /tmp/docker-compose.sha256
          echo "$(cut -d' ' -f1 /tmp/docker-compose.sha256)  /tmp/docker-compose" | sha256sum -c -
          mv /tmp/docker-compose ~/.docker/cli-plugins/docker-compose
          chmod +x ~/.docker/cli-plugins/docker-compose
          docker compose version

      - name: Pull docker images
        run: docker compose pull

      - name: touch .env
        run: touch .env

      - name: pre-commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: make run-pre-commit && make clean-pre-commit

      # setup and install everything and run tests
      - name: up
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: make up

      - name: Set correct permissions for tests
        run: |
          sudo chmod -R 777 ${{ github.workspace }}

      - name: Create reports output folder
        run: |
          mkdir -p ${{ github.workspace }}/reports
          chmod -R 777 ${{ github.workspace }}/reports

      - name: tests
        run: |
          docker compose run --rm -v ${{ github.workspace }}/reports:/reports dev pytest tests --tb=auto --junitxml=/reports/test-results.xml

      - name: Upload pytest test results
        uses: actions/upload-artifact@v7
        with:
          name: pytest-results
          path: ${{ github.workspace }}/reports/**/*.xml
        # Use always() to always run this step to publish test results when there are test failures
        if: ${{ always() }}

      - name: Verify reports are generated
        if: ${{ always() }}
        run: ls -halt ${{ github.workspace }}/reports/

      # Publish all test results in the GitHub UI
      - name: Surface failing tests
        if: always()
        uses: pmeier/pytest-results-action@fdc7f18d9934e38aca411ca9557e6577bd25ca9c # v0.9.0 https://github.com/pmeier/pytest-results-action
        with:
          # A list of JUnit XML files, directories containing the former, and wildcard
          # patterns to process.
          # See @actions/glob for supported patterns.
          path: ${{ github.workspace }}/reports/*.xml

          # (Optional) Add a summary of the results at the top of the report
          summary: true

          # (Optional) Select which results should be included in the report.
          # Follows the same syntax as `pytest -r`
          display-options: fEX

          # (Optional) Fail the workflow if no JUnit XML was found.
          fail-on-empty: true

          # (Optional) Title of the test results section in the workflow summary
          title: Test results

      - name: Cleanup
        if: always()
        run: |
          docker compose down -v --remove-orphans

      - name: 'Cleanup build folder'
        if: always()
        run: |
          ls -la ./
          sudo rm -rf ${{ github.workspace }}/*
          ls -la ./
